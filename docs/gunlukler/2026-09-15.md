---
title: Çalışma günlüğü 2026-09-15
date: 2026-09-15
tags:
  - gunluk
  - faz-0
---

# 2026-09-15

Üst notlar: [[00-MASTER-PRD]] · [[07-KARAR-GECMISI-VE-DEGISIM-GUNLUGU]] · [[08-OBSIDIAN-KULLANIM-REHBERI]]

## Faz 0 — repo iskeleti ve hafıza altyapısı

- [x] PRD seti `prd/` → `docs/` taşındı; `docs/` Obsidian vault olarak açılacak
- [x] Kök `CLAUDE.md` yazıldı (00 master, 06 tasarım, 07 günlük, 09 sözleşme kuralları)
- [x] `git init` (branch `main`), `.gitignore`, `docker-compose.yml` (Redis)
- [x] Postgres DB'leri: `fabrmatch_store`, `fabrmatch_network`
- [x] Skill'ler kuruldu: Medusa (6), Astro, FastAPI, Obsidian markdown, Stripe Connect, graphify
- [ ] graphify ilk grafik (kod oluştukça)

## Doğrulanan sürümler

| Paket | Sürüm |
|---|---|
| `@medusajs/medusa` | 2.21.0 |
| `@mercurjs/core` / `@mercurjs/cli` | 2.3.4 (peer: medusa ≥ 2.20.1) |
| `@adonisjs/core` | 7.5.0 (Node ≥ 24) |
| `astro` | 7.3.2 |

## Faz 1 — Sistem A (çekirdek tamamlandı)

- [x] `create-mercur-app` ile `apps/store` (basic şablon, Next.js storefront atlandı)
- [x] npm kurulum sorunu: peer çakışması → `legacy-peer-deps`; eksik `ajv@8` eklendi
- [x] `packages/api/.env` (DB, Redis, CORS'a Astro portu 4321 eklendi)
- [x] Migration (198 tablo), Fabrmatch seed'i: TR/EU bölgeleri, 3D baskı kategorileri,
      Malzeme/Renk eksenleri, `print_profile` metadata'lı 8 ürün, 2 satıcı, 28 teklif
- [x] Vitrin anahtarı "Fabrmatch storefront" — tek satış kanalı (bkz. tuzaklar)
- [x] `productionSync` modülü: `production_request` + `payout_instruction` (defter)
- [x] `order.placed` → talep oluşturma → Sistem B'ye gönderim; başarısızsa 5 dk'da bir tekrar
- [x] `POST /webhooks/production-status`: zaman damgalı HMAC, ileri-giden durum, idempotent ödeme talimatı
- [x] Smoke test (gerçek sepet akışı + 6 webhook senaryosu) geçti; imza birim testleri 5/5; `tsc` temiz
- [ ] Stripe Connect transferi, uzlaştırma → Faz 5
- [ ] Admin panelinde üretim talepleri görünümü → sonra

> [!warning] Karşılaşılan tuzaklar
> - `query.graph` order kalemlerinde `items.quantity` boş döner; `items.detail.quantity` gerekir.
> - Medusa'nın varsayılan publishable key'i ikinci kanala bağlanınca `POST /store/carts` 400 verir.
> - Workflow hataları `Error` örneği değil → `src/lib/error-message.ts`.

## Faz 2 — Geometri servisi (tamamlandı)

- [x] `services/geometry`: FastAPI + trimesh + Embree, `uv` ile
- [x] `/v1/analyze` (multipart), `/v1/analyze-url` (presigned URL, host allowlist), `X-Service-Key`
- [x] Üretilebilirlik: kapalı olmayan mesh, yazıcı hacmi (döndürmeli), ince duvar hata/uyarı, overhang, küçük model
- [x] 18 test geçiyor, Ruff temiz
- [x] Performans: 327k yüz 0,6 sn, 1,3M yüz 2,8 sn (hedef 30 sn)

## Faz 3 hazırlık

- [x] `apps/manufacturer-network`: AdonisJS v7 React starter kit (Inertia 3, React 19, Lucid, vine)
- [x] Kuyruk kararı: `@adonisjs/queue` (bkz. günlük)

> [!note] Mercur sürümü
> PRD "Mercur 2.0" diyor; güncel kararlı sürüm 2.3.4. Aynı 2.x mimarisi olduğu için
> 2.3.4 kullanılıyor — bkz. [[07-KARAR-GECMISI-VE-DEGISIM-GUNLUGU]].
