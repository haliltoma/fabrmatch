---
title: Çalışma günlüğü 2026-09-16
date: 2026-09-16
tags:
  - gunluk
  - faz-3
---

# 2026-09-16

Önceki gün: [[2026-09-15]] · Üst notlar: [[00-MASTER-PRD]] · [[03-PRD-URETICI-AGI]] · [[04-PRD-GUVEN-VE-KALITE]] · [[09-API-SOZLESMESI]]

## Faz 3 — Sistem B üretici ağı

### Altyapı
- [x] Postgres (`fabrmatch_network`, test: `fabrmatch_network_test`), `@adonisjs/redis`, `@adonisjs/queue` (elle yapılandırıldı)
- [x] Test ortamı: sync kuyruk, global transaction, `forceExit`, Japa API client + auth/session/shield/inertia eklentileri

### Veri modeli
- `manufacturers` (anonim `public_code`, malzemeler, kapasite, gram/saat ücreti, reputasyon girdileri)
- `production_requests` (sözleşme kimliği `pr_…`, kalem başına idempotent, `print_estimate`)
- `match_offers` (seçim nedeni `score` / `exploration` / `protection` ve skor dökümü saklanır)
- `outbound_webhook_events` (gönderen defteri, `next_attempt_at`), `payout_instructions` (`pi_…`)

### Eşleştirme motoru ([[04-PRD-GUVEN-VE-KALITE]])
- [x] Kalite %35 · fiyat %25 · yakınlık %20 · müsaitlik %20
- [x] Soğuk başlangıç = platform ortalaması · %20 keşif · %30 bölgesel tavan · ilk 15 siparişte düşük riskli işlerde bonus
- [x] "Yeni üretici ilk 15 işte en az 1 sipariş alır" başarı kriteri birim testle doğrulanıyor

### Sözleşme uçları ([[09-API-SOZLESMESI]])
- [x] `POST /api/v1/production-requests` — Bearer + Idempotency-Key, 201/200, 401, 422
- [x] `GET /api/v1/region-capability`
- [x] Webhook gönderimi: v1 HMAC, sıralı teslim, 1/5/30/120 dk tekrar, 4xx'te `failed`, teslimde `payout_instruction` → `sent`
- [x] Ortak imza test vektörü iki sistemde de aynı sonucu veriyor

### Üretici paneli (Inertia + React, [[06-PRD-TASARIM-SISTEMI]])
- [x] Honey B2B şeridi, Tabler outline ikonlar, 400/500 ağırlık, ekranda tek solid sage buton
- [x] Başvuru → `pending`; pano (3 dengeli istatistik, teklifler, devam eden işler); iş detayı (malzeme verimliliği çubuğu + 4 veri, adım listesi)
- [x] Giriş/kayıt sayfaları Türkçe, 06 sınıflarıyla
- [ ] Panel fonksiyonel testleri çalıştırılıp doğrulanacak

### Zamanlanmış işler (Faz 3 kapanışı)
- [x] `SweepDueWebhooks` (her dakika) — `next_attempt_at` zamanı gelmiş webhook'ları tekrar dener
- [x] `SweepMatching` (2 dakikada bir) — yanıtsız teklifleri süresi dolmuş sayıp talebi
      eşleştirmeye geri döndürür (rakibi bir sonraki denemede hariç tutulur) ve daha önce
      uygun üretici bulunamayan talepleri periyodik olarak yeniden dener
- [x] `start/scheduler.ts` sadece `node ace serve` altında yüklenir (`environment: ['web']`);
      `@boringnode/queue`'nun `Job.schedule({}).id(...).cron(...).timezone('UTC').run()` API'si
      kullanıldı, kararlı `.id()` ile yeniden başlatmalar kayıt çoğaltmıyor
- [x] Doğrulama: kısa bir `node ace serve` sonrası Redis'te `schedules::sweep-due-webhooks`
      ve `schedules::sweep-matching` anahtarları göründü

**Faz 3 TAMAMLANDI.** Sıradaki: Faz 4 — Astro storefront.

### Test durumu
- Birim + fonksiyonel: **33/33 geçti**, `npm run typecheck` temiz

> [!warning] `routes.d.ts` tuzağı
> Yeni `.as('panel.…')` route'ları eklendikten sonra `node ace test` her seferinde
> `.adonisjs/server/routes.d.ts`'i yenilemiyor (router test sürecinde commit olmadan
> `app.ready()` geçiyor). Çözüm: `node ace serve`'i birkaç saniye açıp kapatmak (ilk
> isteği tetikler) — bkz. hafıza notu.

## Faz 4 — Astro storefront (başlıyor)

> [!note] Tasarım sistemi eki
> Form hataları için 06'ya `danger` renk grubu eklendi — bkz. [[07-KARAR-GECMISI-VE-DEGISIM-GUNLUGU]].

## Faz 4 — Astro storefront (çekirdek tamamlandı)

### Kurulum
- [x] `apps/storefront`: Astro 7 + React + `@astrojs/node` (Cloudflare'e geçiş tek satır, bkz. 07)
- [x] `@medusajs/js-sdk` + `@medusajs/types` (building-storefronts skill'i takip edildi:
      SDK metotları built-in uçlar için, Mercur'un değiştirdiği uçlar (`offer_id` ile
      satır ekleme, satıcıya göre gruplanmış kargo seçenekleri, `order_group` dönen
      complete) için `sdk.client.fetch()` — tipler doğrulanmadan asla varsayılmadı)
- [x] `astro:env` ile tipli ortam değişkenleri; geometri servisi anahtarı sadece sunucu bağlamında

### Sayfalar
- [x] Anasayfa, tüm ürünler, 8 kategori sayfası, 8 ürün sayfası — hepsi build-time statik
- [x] Ürün detayı: Malzeme/Renk seçici → varyant → buy-box (diğer satıcılar canlı çekiliyor)
      + malzeme verimliliği çubuğu (gerçek seed verisiyle)
- [x] Sepet/checkout tamamen istemci tarafında (React island) — adres → kargo (satıcı
      başına) → ödeme → sipariş tamamlama; sunucu render gerekmiyor
- [x] Özel tasarım sayfası: dosya yükle → sunucu vekili (`/api/analyze`, `prerender=false`)
      → geometri servisi → anlık fiyat/malzeme tahmini

### Uçtan uca doğrulama (gerçek tarayıcı, gerçek Sistem A)
- [x] Ürün sayfasında varyant seçimi, buy-box'tan sepete ekleme, header rozetinin
      `aria-live` ile güncellenmesi
- [x] Tam checkout: adres kaydı → kargo seçimi → ödeme onayı → "Siparişiniz alındı"
      (gerçek `order_group` kimliğiyle) — `order.placed` Sistem A tarafında yeni bir
      `production_request` açtığını doğruladı (Sistem B kapalı olduğu için `dispatch_failed`,
      beklenen davranış)
- [x] `/api/analyze` gerçek bir STL ile uçtan uca test edildi (TRY fiyat, malzeme/süre tahmini)

### Karşılaşılan hatalar (düzeltildi)
- Ürün/teklif `calculated_price` alanı `region_id` olmadan 400 veriyordu (offers ucu için de aynı kural geçerli — dokümantasyon sadece ürünler için açıkça belirtmişti)
- `.env`'deki vitrin anahtarı yanlışlıkla "Default Publishable API Key"e (2 satış kanallı) işaret ediyordu, sepet oluşturma 400 veriyordu — "Fabrmatch storefront" anahtarına düzeltildi
- Sepet kalemi görseli yoksa 3 sütunlu grid bozuluyordu — görsel yoksa yer tutucu render edilecek şekilde düzeltildi
- `services/geometry`'nin `uv` editable kurulumu bozulmuştu (`rm -rf .venv && uv sync` ile kalıcı çözüldü) — bkz. hafıza notu
- Geometri servisinin varsayılan para birimi USD'ydi, TRY'ye çevrildi (malzeme kg fiyatları da gerçekçi TRY değerlerine güncellendi)

### Bilinçli ertelenen
- Çoklu bölge/dil algılama (MVP: tek TR/TRY)
- Özel tasarımdan sepete ekleme (Sistem A'da tek seferlik ürün/teklif sağlama iş akışı gerektiriyor)
- Hesap girişiyle kalıcı sipariş takibi (şu an sadece checkout sonrası onay ekranı)

**Faz 4 çekirdeği TAMAMLANDI.**

## Faz 5 — Ödeme, escrow, uzlaştırma + uçtan uca test (tamamlandı)

### Komisyon
- [x] Mercur global komisyon oranı `%0 → %30` (`set-platform-commission.ts`, `updateCommissionRatesWorkflow`)

### Üretici ödemesi
- [x] `PayoutProvider` soyutlaması: `manual` (varsayılan, `STRIPE_SECRET_KEY` yoksa —
      checkout'taki `pp_system_default` ile aynı mantık) + `stripe-connect` (gerçek
      `stripe.transfers.create`, birim testlerde sahte Stripe istemcisiyle doğrulandı)
- [x] 09 sözleşmesi: `payout_instruction.manufacturer_account {provider, account_id}` —
      sadece kimlik, Sistem B'de para hareketi yok
- [x] Sistem A: `execute-payout` workflow adımı — hesap yoksa veya sağlayıcı hata
      dönerse talimatı `failed` işaretler, ASLA fırlatmaz (webhook işleme geri
      alınmasın diye)

### Günlük uzlaştırma
- [x] Sistem A: `inbound_webhook_event` (gelen-kutusu defteri, her webhook'ta yazılır)
- [x] Sistem B: `GET /api/v1/webhook-events?since=` (yalnızca sonuçlanmış olaylar)
- [x] Sistem A: `daily-reconciliation` zamanlanmış işi (`0 3 * * *`) — 26 saatlik
      örtüşmeli pencere, sadece Sistem B'nin `delivered` dediği olaylar aday
      (`failed` — ikisi de pes etti — anomali sayılmaz)
- [x] `reconciliation_report` (append-only) + `GET /admin/reconciliation-reports`

### Uçtan uca doğrulama (üç gerçek servis, gerçek tarayıcı)
- [x] Yeni test üreticisi (`FM-74ECE9`) gerçek signup+onboarding akışıyla oluşturuldu,
      `manufacturer:activate` ile onaylandı, `stripe_account_id` elle atandı (demo)
- [x] Storefront'tan gerçek sipariş → Sistem B'ye dispatch → **kuyruk işçisi
      (`node ace queue:work`) gerekli olduğu keşfedildi** (redis sürücüsünde
      eşleştirme job'ı kuyruğa düşüyor, worker koşmadan işlenmiyor) → eşleşme
- [x] Üretici panelinden gerçek tıklamalarla: kabul → üretime başla → kalite kontrol →
      kargo → teslim edildi (iki paralel sipariş, biri Faz 1'den kalma `dispatch_failed`
      bir kaydın Sistem A'nın 5 dakikalık tekrar deneme job'ıyla kendiliğinden
      düzelmesiyle ortaya çıktı — hoş bir tesadüfi doğrulama)
- [x] Her iki `payout_instruction` da `paid`, sağlayıcı `manual`, benzersiz referans
- [x] Kasıtlı bozuk imza → 401 (canlı doğrulandı)
- [x] Uzlaştırma "10/10 eşleşti" → bir olay kasıtlı silinip yeniden çalıştırıldı →
      "1 event(s) ... never received" doğru tespit edildi, rapor admin API'sinde görünür
- [x] Küçük hata düzeltmesi: kalite kontrol adımı boş/geçersiz fotoğrafla sunucu
      hatası (500) veriyordu → dostça hata mesajına çevrildi, test eklendi

### Test durumu
- Sistem B: **38/38** · Sistem A: **18/18** birim testi, `tsc` her iki tarafta temiz

**Faz 5 TAMAMLANDI — beş faz da bitti.** Kalan (bilinçli ertelenen, ayrı iş kalemleri):
üretici Stripe Connect onboarding UI'ı, kanıta dayalı anlaşmazlık çözüm süreci,
özel tasarımdan sepete ekleme, çoklu bölge/dil, hesap girişiyle sipariş takibi.

## Anlaşmazlık çözüm süreci (Faz 5 sonrası ek iş, tamamlandı)

### Escrow penceresi
- [x] Faz 5'teki "delivered gelir gelmez hemen öde" davranışı kaldırıldı — talimat
      artık `received` + `release_at` (`now + 48s`, `PAYOUT_RELEASE_WINDOW_HOURS`)
      ile kaydediliyor, `apply-production-status` iş akışı artık ödemeyi tetiklemiyor
- [x] Yeni `release-due-payouts` job'ı (15 dk) — penceresi dolan ve anlaşmazlığı
      olmayan (`on_hold` OLMAYAN) talimatları otomatik serbest bırakıyor

### Anlaşmazlık modeli ve API
- [x] Yeni `dispute` tablosu (production-sync modülü) — kanıt AYRICA saklanmıyor,
      mevcut `production_request` (fotoğraf/kargo) ve `inbound_webhook_event`
      (olay geçmişi) canlı okunuyor
- [x] `open-dispute` iş akışı: bilinmeyen production_request'i reddeder, dispute
      oluşturur, varsa `received` talimatı `on_hold`a alır
- [x] `resolve-dispute` iş akışı: `manufacturer`/`dismiss` → talimatı serbest
      bırakır (`attemptPayout` execute-payout.ts ile paylaşılıyor); `buyer` →
      `cancelled`; zaten çözülmüş bir anlaşmazlık tekrar çözülemez
- [x] Admin API: `GET/POST /admin/disputes`, `GET /admin/disputes/:id` (kanıt +
      zaman çizelgesi + ilişkili payout_instruction), `POST
      /admin/disputes/:id/resolve`

### Doğrulama
- [x] `npx tsc --noEmit` temiz, birim testleri 19/19 (yeni: `computeReleaseAt`)
- [x] `medusa exec` ile canlı DB'ye karşı uçtan uca: kayıt→hold→üretici-lehine-öde,
      kayıt→hold→alıcı-lehine-iptal, pencere-dolup-anlaşmazlıksız→otomatik-öde,
      çift-çözüm-reddi, bilinmeyen-production_request-reddi — hepsi başarılı
- [x] Admin HTTP uçları gerçek admin oturumuyla `curl` ile ayrıca doğrulandı
- [x] Geçici doğrulama scriptleri (`test-dispute-flow.ts`, `test-release-and-guards.ts`)
      kullanımdan sonra silindi (proje konvansiyonu)

### Bilinçli ertelenen
- Alıcı hesabı/girişi yok — anlaşmazlık açma şimdilik yalnızca admin üzerinden
- Zaten `paid` olmuş bir talimatın sonradan açılan anlaşmazlıkla gerçek parasal
  geri alınması (Stripe transfer reversal) uygulanmadı — manuel takip gerekir

## Faz 5 sonrası — Storefront hesap girişi + sipariş takibi

### Kapsam
- [x] `lib/auth.ts` — JWT oturumu SDK localStorage varsayılanıyla ([[07-KARAR-GECMISI-VE-DEGISIM-GUNLUGU]]
      2026-09-16 girdisi): `getSessionCustomer` (401 → token temizle + null),
      `loginCustomer`, `registerCustomer` (2 adım: register → customer.create → login),
      `logoutCustomer`, `AUTH_CHANGED_EVENT` ([[cart-events]] deseni)
- [x] `lib/mercur-custom.ts` — `OrderGroup`/`OrderGroupOrder`/`OrderGroupItem` tipleri,
      `listOrderGroups`, `getOrderGroup` (404 → null), `transferCart`, complete
      yanıtına `customer_id`. Plan aksine: Mercur'un varsayılan order-group fields
      listesi Medusa'nın "en fazla 3 ilişki" sınırını aştığı için 400 veriyor —
      3 ilişkide kalan curated `ORDER_GROUP_FIELDS` gönderiliyor. Ayrıca Mercur'un
      order modelinde yalnızca tek `status` var (fulfillment/payment status kolonları yok)
- [x] `/giris` + `AuthPage` — login/register toggle, Türkçe hatalar, zaten girişli koruması
- [x] `/hesap` + `AccountPage` — profil kartı + çıkış, "Siparişlerim" listesi
      (`fm-entry-card--secondary` yeniden kullanımı, [[06-PRD-TASARIM-SISTEMI]])
- [x] `/hesap/siparis/[id]` + `OrderGroupDetail` — prerender=false; satıcı başına bölüm,
      `fm-badge--{sage|muted}` rozetleri (06'ya önce eklendi), TR durum sözlüğü
      (`lib/order-status.ts`, bilinmeyen değer ham gösterilir)
- [x] Header'a `AccountLink` adası (CartCount deseni); girişsiz `/giris`, girişli `/hesap`
- [x] `CartPage` — ilk yüklemede bir kez `transferCart` (hata → sessiz misafir devam),
      e-posta ön-doldurma, onayda girişliye "siparişinizi görüntüleyin →" linki

### Doğrulama
- [x] `npx astro check` 0 hata, `npm run build` temiz; dev sunucuda `/giris`, `/hesap`,
      `/hesap/siparis/[id]`, `/sepet` 200
- [x] Backend akışı SDK scriptiyle canlı uçtan uca (headless, token elle taşınarak —
      tarayıcıda SDK otomatik yapıyor): kayıt (2 adım) → giriş → misafir sepette
      customer_id null → transfer sonrası customer_id + e-posta üzerine yazılmış →
      sipariş → complete yanıtı customer_id eşleşiyor → `/store/order-groups` listesi
      ve detay → başka müşterinin group id'si ve olmayan id → 404 (→ "Sipariş bulunamadı.")
- [x] Gerçek tarayıcıda (Playwright + sistem Chrome, headless) tam tur: kayıt → /hesap
      yönlendirme → header "hesabım" → yenilemede oturum kalıcı → çıkış (header "giriş")
      → yeniden giriş → sepete ekle → /sepet'te e-posta müşteri e-postasıyla öndolduruldu
      → adres/kargo/ödeme → onayda "siparişinizi görüntüleyin →" → detay (Atölye Mersin,
      "bekliyor" rozeti) → /hesap listesi (#display_id) → yabancı id → "Sipariş bulunamadı."
      → misafir bağlamda /hesap boş durumu + header "giriş". Geçici script silindi.

### Bilinçli kapsam dışı
- Token refresh (store JWT uzun ömürlü; süre dolunca sessiz çıkış), login öncesi
  sepetin /sepet ziyaret edilmemişse bağlanmaması (misafir sipariş olur — kabul),
  alıcı tarafından dispute açma (ayrı iş), JWT'nin localStorage'da durması (XSS — MVP kabul)

## Özel tasarımdan sepete ekleme (ertelenen iş #2 tamamlandı)

Detaylı karar kaydı: [[07-KARAR-GECMISI-VE-DEGISIM-GUNLUGU]] (2026-09-16, "Özel
tasarımdan sepete ekleme"). [[00-MASTER-PRD]] §4.1 ertelenen listesinden çıkarıldı;
§3 ADR tablosu değişmedi. [[09-API-SOZLESMESI]] DOKUNULMADI.

### Sistem A (apps/store/packages/api)
- `src/lib/custom-design-listing.ts`: saf yardımcılar — markup parse (geçersiz→1.0,
  >100→clamp), fiyat (2 ondalık), metadata (opak `sha256:` referansı + print_profile +
  print_params), varyant başlığı ("PLA · 2 adet"), SKU ve handle üretimi. 10 unit test
  (`src/lib/__tests__/custom-design-listing.unit.spec.ts`).
- `src/workflows/steps/resolve-platform-seller.ts`: `PLATFORM_SELLER_EMAIL` → onaylı
  satıcı (Mercur SellerStatus.OPEN) + member + stok konumu (seller↔stock_locations
  link) + "Marketplace Shipping" profili (yoksa createShippingProfilesWorkflow).
- `src/workflows/steps/create-custom-design-listing.ts`: createProductsWorkflow
  (PUBLISHED + Mercur "Malzeme" attribute bağlantısı + benzersiz handle) → varyantı
  geri sorgula → createOffersWorkflow (TRY tek fiyat, sanal stok 1M).
- `src/workflows/create-custom-design-listing.ts` + `src/api/store/custom-designs/`
  (zod middleware + POST route: üretilemez→422, params≠cost adedi→422, aksi halde 201).
- `.env.template`/`.env`: `PLATFORM_SELLER_EMAIL`, `CUSTOM_DESIGN_MARKUP=1.0`.

### Storefront (apps/storefront/src)
- `lib/analysis-types.ts`: `params` + `analysis_ms` eklendi (geometri şemasıyla birebir).
- `lib/cart-helpers.ts`: `getOrCreateCartId` VariantOfferPicker'dan taşındı (paylaşımlı).
- `pages/api/custom-design.ts`: analiz + ad → Sistem A `POST /store/custom-designs` proxy.
- `components/CustomDesignForm.tsx`: analiz girdisi değişirse sonuç sıfırlanır;
  üretilebilirken tasarım adı + "sepete ekle" → offer_id → sepet → "Sepete eklendi.
  sepete git →" (aria-live). `lib/store-data.ts` + `lib/mercur-types.ts`:
  katalog `metadata.custom_design` ürünlerini listelemez, direkt URL 404.

### Canlı E2E (Playwright headless, sistem Chrome)
Katalog ürünü sepete → `/ozel-tasarim` üretilebilir kutu STL (PLA, 2 adet) → analiz
₺16,48 → ad gir → sepete ekle → `/sepet` 2 satır ("PLA · 2 adet" + katalog), toplam
₺636,48 → adres/kargo/ödeme → sipariş `og_01M2MX37AR4FSB5C9X7N8EKCST`. DB: üründe
`design_reference=sha256:…` + `custom_design=true`, production_request açıldı
(material=PLA, color=null, print_estimate dolu) ve `matching_in_progress` (Sistem B
dispatch ✓). Negatif: açık (watertight olmayan) mesh'te sepete ekle formu yok;
`PLATFORM_SELLER_EMAIL` boşken arayüz "Sepete eklenemedi" + backend logu net.
Geçici scriptler silindi.

### Öğrenilenler (plan sapmaları)
- Medusa `addToCartWorkflow` sepete eklemede ürünü PUBLISHED zorluyor → PROPOSED-gizli
  ürün planı yerine PUBLISHED + vitrin kataloğunda `custom_design` metadata filtresi.
- Mercur onaylı satıcı statusu "open" (SellerStatus.OPEN); "approved" diye değer yok.
- Mercur createProductsWorkflow eksen seçeneklerini Mercur product attribute'larından
  bağlar; `attributes` verilmezse varyant seçenek uyuşmazlığı ("2 provided vs 1").
- Medusa handle'ı title'dan üretir → aynı isimli ikinci özel tasarım çakışırdı; handle
  açıkça `custom-<hash12>-<rand8>` verilir.
- `medusa develop` `.env`'i hot-reload etmez; env değişikliğinde restart şart.
- services/geometry `cost.currency` = "TRY" (Intl biçimi); Sistem A şeması lowercase
  transform ile `try` doğrular.

### Bilinçli kapsam dışı / ertelenen
- STL dosyası saklanmıyor (opak hash yeterli; presigned URL ayrı iş).
- Satır kalemi quantity=1: Sistem B siparişte 1 adet görür; gerçek adet varyant
  başlığında/metadata'da. Satır bazlı miktar düzeltmesi ayrı iş.
- Tek seferlik ürünler Sistem A'da silinmiyor (soft-delete ile kalır); yaşam döngüsü
  (ör. sipariş sonrası temizlik) ayrı iş.
